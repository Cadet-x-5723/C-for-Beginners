#include<stdio.h>

int main(){
    int a; // declaration of the integer
    scanf("%d", &a); //input command
    printf(" the input is %d", a);
    return 0;
}

// WE CAN USE < ALT+SHIFT+ DOWN ARROW > FOR DUPLICATING A CODE LINE