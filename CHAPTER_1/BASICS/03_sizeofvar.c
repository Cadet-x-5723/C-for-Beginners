#include <stdio.h>

int main()
{
    printf("size of integer: %zu Bytes \n", sizeof(int));
    printf("size of floats: %zu Bytes \n", sizeof(float));
    printf("size of character: %zu Bytes \n", sizeof(char));
    return 0;
}
