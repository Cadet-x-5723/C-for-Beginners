#include<stdio.h>

int main(){

    int a= 7;
    int b=7;
    printf("the sum is : %d", a+b);
    return 0;
}

// in the similar way, we can even code it as :

#include<stdio.h>

int main(){

    int a= 8;
    int b=10;
    int sum= a+b;
    printf("the sum is : %d", sum);
    return 0;
}

// modulus operator can't be used in float values